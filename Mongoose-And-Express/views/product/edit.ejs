<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edi product</title>
</head>
<body>
    <h1>Edit the product</h1>
    <form action="/products" method="POST">
        <label for="name"></label>
        <input type="text" name="name" id="name" placeholder="name" value="<%=foundProduct.name%>">
        <label for="price"></label>
        <input type="number" name="price" id="price" placeholder="price" value="<%=foundProduct.price%>">
        <label for="category"></label>
        <select name="category" id="category">
            <% for(let category of categories) { %>
                <option value="<%=category%>" <%= foundProduct.category === category ? 'selected' : '' %>><%=category%></option>
            <% } %>
        </select>
        <label for="quantity"></label>
        <input type="number" name="quantity" id="quantity" placeholder="quantity" value="<%=foundProduct.quantity%>">
        <button>Submit</button>
    </form>
    <a href="/products/<%=foundProduct._id%>">Cancel</a>
</body>
</html>