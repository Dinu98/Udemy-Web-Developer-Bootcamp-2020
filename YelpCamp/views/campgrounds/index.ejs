<% layout('layouts/boilerplate')%>
    <h1>Index page</h1>
    <a href="/campgrounds/new">Add a campground</a>

    <div id="map" style="width: 100%; height: 500px;"></div>

    <% for(let campground of campgrounds) { %>
        <div class="card">
            <div class="row">
                <div class="col-md-4">
                    <% if(campground.images[0]) { %> 
                    <img src="<%=campground.images[0].url%>" alt="" class="img-fluid">
                    <% } else { %>
                    <img src="https://res.cloudinary.com/deqyehnld/image/upload/v1607211471/YelpCamp/wbrc3gcwe29s0ea8jl7b.jpg" alt="" class="img-fluid">
                    <% } %>  
                </div>
                <div class="col-md-8">
                    <div class="card-body">
                        <h5 class="card-title"><%=campground.name%></h5>
                        <p class="card-text"><%=campground.description%></p>
                        <p class="card-text">
                            <small class="text-muted"><%=campground.location%></small>
                        </p>
                        <a href="/campgrounds/<%=campground._id%>" class="btn btn-primary">View <%=campground.name%></a>
                    </div>
                </div>
            </div>
        </div>
    <% } %>

<script> 
    const mapToken = '<%-process.env.MAP_BOX_TOKEN%>';
</script>
<script src="/javascripts/clusterMap.js"></script>
